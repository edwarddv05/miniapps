export type MiniappDestination = 'library' | 'schedule' | 'downloader';

export const UI = {
  contentWidth: 720,
  pageInset: 20,
  sheetActionInset: 20,
  circleControlSize: 44,
  groupRadius: 20,
  iconRadius: 12,
} as const;
